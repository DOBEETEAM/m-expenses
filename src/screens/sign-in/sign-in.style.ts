import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  logoContainer: {
    marginBottom: 20,
    width: 100,
    height: 100,
  },
  imageContainer: {
    width: '100%',
    height: '100%',
  },
});
