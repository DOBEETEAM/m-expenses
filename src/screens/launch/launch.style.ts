import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  screenContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  logoContainer: {
    marginBottom: 40,
    width: 200,
    height: 200,
  },
  imageContainer: {
    width: '100%',
    height: '100%',
  },
});
