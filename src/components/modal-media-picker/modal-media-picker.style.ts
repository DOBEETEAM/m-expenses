import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btnOptionContainer: {
    marginHorizontal: 15,
    marginBottom: 15,
    marginTop: 50,
  },
  btnContent: {
    height: 90,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginHorizontal: 8,
  },
});
